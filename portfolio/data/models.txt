# Envoy is the only agent used by the ask section. It owns provider selection,
# authentication, tool policy, and fallback behavior in ai-sdk/envoy.json.
#
# The names here are the model ids passed to Envoy. The file is mounted by
# docker-compose so the order can be changed without rebuilding the image.

tier  envoy codex
  command  envoy
  pin      flag --model
  tools    env ENVOY_MCP_HTTP
  secrets  none
  model    gpt-5.6-luna

tier  envoy ollama
  command  envoy
  pin      flag --model
  tools    env ENVOY_MCP_HTTP
  secrets  OLLAMA_API_KEY
  model    qwen3.5:397b
  model    gpt-oss:120b
